# Core async dependencies (optional - will use fallbacks if not available)
aiohttp>=3.8.0
aiofiles>=22.1.0
requests>=2.28.0
beautifulsoup4>=4.11.0
urllib3>=1.26.0

# GUI dependencies (optional - will use fallbacks if not available)
Pillow>=9.0.0

# Core dependencies (required)
python-dateutil>=2.8.0
certifi>=2022.12.0
charset-normalizer>=3.0.0
idna>=3.4

# Optional async dependencies
async-timeout>=4.0.0
multidict>=6.0.0
yarl>=1.8.0

# Development and testing dependencies
xmltodict>=0.13.0